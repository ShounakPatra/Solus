<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LocalMeshAI" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_bg</item>
    </style>

    <style name="Theme.LocalMeshAI.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_edge</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_edge</item>
        <item name="postSplashScreenTheme">@style/Theme.LocalMeshAI</item>
        <item name="android:windowBackground">@drawable/splash_bg</item>
        <item name="android:navigationBarColor">@color/splash_edge</item>
        <item name="android:statusBarColor">@color/splash_edge</item>
    </style>
</resources>
